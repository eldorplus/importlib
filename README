A complete backport to 2.7 (and 3.x) of the importlib package from Python 3.  Compatibility is intended to match only the public, non-deprecated API in Python 3.5.

See https://docs.python.org/3.5/library/importlib.html.
